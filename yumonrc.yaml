tasks:
  - name: Hello world
    alias:
      - greet
    description: Show a "Hello world"
    action: echo "Hello world"
    
  - name: build
    alias:
      - b
    description: Run "pnpm build" command
    action: pnpm build
